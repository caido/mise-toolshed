#!/usr/bin/env sh
# Dev convenience: same entrypoint as the mise-installed wrapper.
set -e
ROOT="$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)"
exec "$ROOT/scripts/skills" \
	--url "https://github.com/caido/ai-ops" \
	--destination "${SKILLS_DESTINATION:-$PWD}"
